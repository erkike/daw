#dentro de la carpeta docker

docker build -t seriescarpincho-mysql ./mysql
docker run seriescarpincho-mysql

# en otro terminal y en la carpeta daw

docker build -t seriescarpincho .

docker run -p 8080:8080 seriescarpincho
